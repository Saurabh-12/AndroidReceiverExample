<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".PendingIntentExampleActivity" >

    <Button
        android:id="@+id/start_pending_intent_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="172dp"
        android:text="Start PendingIntent" />

    <EditText
        android:id="@+id/put_delay_etv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/start_pending_intent_btn"
        android:layout_alignParentTop="true"
        android:hint="Put Delay for PI(in second)"
        android:layout_alignRight="@+id/start_pending_intent_btn"
        android:layout_marginTop="63dp"
        android:ems="10" />

</RelativeLayout>